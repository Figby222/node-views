<head>
    <link rel="stylesheet" href="./styles.css">
</head>
<html>
    <body>
        <%= message %>
        <%- include("navbar", { links: links }) %>
        <ul>
            <% users.forEach((user) => { %>
                <%- include("users/user", { user: user }); %>
            <% }) %>
        </ul>
        <%- include("footer", { secretFooterMessage: secretFooterMessage }) %>
    </body>
</html>